# Demo Queries

The maintained source of truth for fixture-aligned live smoke queries is:

- [`docs/test-queries.md`](test-queries.md)

This file is intentionally kept as a pointer to avoid maintaining two overlapping query lists.
